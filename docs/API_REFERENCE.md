# Advanced Crypto Trading Bot API Reference

The Advanced Crypto Trading Bot provides a REST API to access real-time trading data, signals, news, and system health. The API is primarily used to power the Web Dashboard.

## Authentication
Currently, the REST API endpoints are public and do not require authentication. They are designed for local dashboard access or securely firewalled network access.

## Rate Limiting
Requests to the `/api/*` endpoints are rate-limited to **100 requests per minute per IP address**.
If you exceed this limit, you will receive a `429 Too Many Requests` response.

```json
{
  "error": "Too many requests, please try again later."
}
```

## Pagination
Endpoints that return lists (such as trades, signals, and news) support pagination via the `limit` query parameter (defaulting to 50 for trades, and 20 for signals and news).

## General Error Handling
Server errors return a `500 Internal Server Error` status with a JSON payload:

```json
{
  "error": "An internal server error occurred"
}
```

---

## Endpoints

### Get Dashboard Data
Retrieves a complete snapshot of the bot's current state, including recent trades, signals, news, portfolio, and stats.

**GET** `/api/dashboard`

**Example Request:**
```bash
curl -X GET http://localhost:3000/api/dashboard
```

**Example Response (200 OK):**
```json
{
  "trades": [
    {
      "id": "trade_123",
      "symbol": "BTCUSDT",
      "action": "BUY",
      "price": 60000,
      "quantity": 0.1,
      "timestamp": "2023-10-01T12:00:00.000Z",
      "profit": 0,
      "status": "OPEN"
    }
  ],
  "signals": [
    {
      "symbol": "ETHUSDT",
      "action": "BUY",
      "price": 2500,
      "confidence": 0.85,
      "timestamp": "2023-10-01T12:05:00.000Z",
      "indicators": {
        "rsi": 40,
        "macd": { "value": 1.5, "signal": 1.2 },
        "bbands": { "upper": 2600, "middle": 2500, "lower": 2400 }
      }
    }
  ],
  "news": [
    {
      "symbol": "BTC",
      "title": "Bitcoin Surges Past 60k",
      "sentiment": "BULLISH",
      "impact": "HIGH",
      "timestamp": "2023-10-01T11:00:00.000Z"
    }
  ],
  "portfolio": {
    "totalValue": 10500,
    "positions": [
      {
        "symbol": "BTCUSDT",
        "quantity": 0.1,
        "averagePrice": 60000,
        "currentPrice": 61000,
        "pnl": 100,
        "pnlPercentage": 1.66
      }
    ],
    "performance": {
      "totalPnl": 500,
      "totalPnlPercentage": 5.0,
      "winRate": 0.65,
      "totalTrades": 20
    }
  },
  "stats": {
    "uptime": 3600000,
    "totalCommands": 150,
    "activeUsers": 5,
    "lastUpdate": "2023-10-01T12:10:00.000Z"
  },
  "openTrades": [
    {
      "id": "trade_123",
      "symbol": "BTCUSDT",
      "action": "BUY",
      "price": 60000,
      "quantity": 0.1,
      "timestamp": "2023-10-01T12:00:00.000Z",
      "profit": 0,
      "status": "OPEN"
    }
  ]
}
```

### Get Trades
Retrieves the most recent trades executed by the bot.

**GET** `/api/trades`

**Query Parameters:**
| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `limit` | `number` | `50` | Maximum number of trades to return |

**Example Request:**
```bash
curl -X GET "http://localhost:3000/api/trades?limit=1"
```

**Example Response (200 OK):**
```json
[
  {
    "id": "trade_124",
    "symbol": "ETHUSDT",
    "action": "SELL",
    "price": 2600,
    "quantity": 1,
    "timestamp": "2023-10-01T13:00:00.000Z",
    "profit": 100,
    "status": "CLOSED"
  }
]
```

### Get Open Trades
Retrieves all currently open trades.

**GET** `/api/trades/open`

**Example Request:**
```bash
curl -X GET http://localhost:3000/api/trades/open
```

**Example Response (200 OK):**
```json
[
  {
    "id": "trade_123",
    "symbol": "BTCUSDT",
    "action": "BUY",
    "price": 60000,
    "quantity": 0.1,
    "timestamp": "2023-10-01T12:00:00.000Z",
    "status": "OPEN"
  }
]
```

### Get Signals
Retrieves recent trading signals generated by the bot's analysis engine.

**GET** `/api/signals`

**Query Parameters:**
| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `limit` | `number` | `20` | Maximum number of signals to return |

**Example Request:**
```bash
curl -X GET "http://localhost:3000/api/signals?limit=1"
```

**Example Response (200 OK):**
```json
[
  {
    "symbol": "SOLUSDT",
    "action": "HOLD",
    "price": 140,
    "confidence": 0.5,
    "timestamp": "2023-10-01T12:00:00.000Z",
    "indicators": {}
  }
]
```

### Get News
Retrieves recent crypto news articles and their sentiment analysis.

**GET** `/api/news`

**Query Parameters:**
| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `limit` | `number` | `20` | Maximum number of news items to return |

**Example Request:**
```bash
curl -X GET "http://localhost:3000/api/news?limit=1"
```

**Example Response (200 OK):**
```json
[
  {
    "symbol": "ETH",
    "title": "Ethereum Upgrade Complete",
    "sentiment": "BULLISH",
    "impact": "HIGH",
    "timestamp": "2023-10-01T10:00:00.000Z"
  }
]
```

### Get Portfolio
Retrieves the current portfolio value, open positions, and historical performance metrics.

**GET** `/api/portfolio`

**Example Request:**
```bash
curl -X GET http://localhost:3000/api/portfolio
```

**Example Response (200 OK):**
```json
{
  "totalValue": 10500,
  "positions": [
    {
      "symbol": "BTCUSDT",
      "quantity": 0.1,
      "averagePrice": 60000,
      "currentPrice": 61000,
      "pnl": 100,
      "pnlPercentage": 1.66
    }
  ],
  "performance": {
    "totalPnl": 500,
    "totalPnlPercentage": 5.0,
    "winRate": 0.65,
    "totalTrades": 20
  }
}
```

### Get Bot Stats
Retrieves operational statistics about the bot instance.

**GET** `/api/stats`

**Example Request:**
```bash
curl -X GET http://localhost:3000/api/stats
```

**Example Response (200 OK):**
```json
{
  "uptime": 3600000,
  "totalCommands": 150,
  "activeUsers": 5,
  "lastUpdate": "2023-10-01T12:10:00.000Z"
}
```

### Get Internal API Health
Retrieves a simple health check status of the internal API.

**GET** `/api/health`

**Example Request:**
```bash
curl -X GET http://localhost:3000/api/health
```

**Example Response (200 OK):**
```json
{
  "status": "OK",
  "timestamp": "2023-10-01T12:15:00.000Z",
  "uptime": 3600
}
```

### Get Detailed System Health
Retrieves a comprehensive snapshot of the bot's system health, including individual components and resource usage.

**GET** `/health`

**Example Request:**
```bash
curl -X GET http://localhost:3000/health
```

**Example Response (200 OK):**
```json
{
  "status": "ok",
  "timestamp": "2023-10-01T12:15:00.000Z",
  "uptime": 3600,
  "memoryUsageMb": 150,
  "requestCount": 150,
  "components": {
    "binanceRest": {
      "status": "ok",
      "lastCheck": 1696162500000,
      "message": "Connected"
    },
    "database": {
      "status": "ok",
      "lastCheck": 1696162500000,
      "message": "Connected"
    }
  }
}
```
