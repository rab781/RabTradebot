## 2024-10-25 - [Docs improvement] API Reference for Web Server
**Learning:** By strictly adhering to the memory directive of directly reading exact TypeScript interfaces (like `BotStateManager` and `HealthMonitor`), I successfully generated completely grounded documentation avoiding hallucination of payload structures.
**Action:** Next time writing API references, I will continue directly running `sed` or `cat` on source files to extract true backend payload interfaces rather than assuming their structure based on typical API standards.
