## 2024-05-24 - API Reference Documentation
**Learning:** Automatically ignoring `docs/` can hide important developer references like API docs. Also, developers need precise request/response examples (like auth, rate limits, pagination) directly mapped from code interfaces.
**Action:** Always modify `.gitignore` to track API documentation and rely on exact TypeScript interfaces rather than hallucinating JSON responses.
