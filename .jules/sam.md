## 2024-06-20 - API Reference documentation and Configuration table standard
**Learning:** README configuration tables should consistently use explicit defaults instead of "Required" columns, and API references must explicitly address cross-cutting concerns like rate-limiting, authentication, and pagination.
**Action:** Always document the exact defaults for each option using a standard "Option | Type | Default | Description" table structure, and ensure one concept per section in API references.
