## 2026-07-19 - Explicit API Reference Documentation
**Learning:** Explicitly documenting API responses and features derived straight from source interfaces (such as `BotStateManager` and `HealthMonitor`) is crucial to avoid hallucinating structures and ensures accurate documentation.
**Action:** Always read the corresponding router code and interface files to explicitly discover and map response structures, authentication details, and configuration features (e.g., limits) before writing any API reference.
