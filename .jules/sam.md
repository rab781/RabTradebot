## 2024-05-18 - Added API Reference Documentation
**Learning:** Automatically ignored documentation folders require updating `.gitignore` rules (e.g., using `!docs/API_REFERENCE.md`) to properly track docs inside ignored directories like `docs/`.
**Action:** When creating new documentation in a project, always verify `.gitignore` first to ensure the documentation will actually be tracked and committed.
